name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify
        run: |
          echo "✅ Code pushed to master"
          echo "📱 Deploy on Manus: https://incontridibakeka.manus.space"
